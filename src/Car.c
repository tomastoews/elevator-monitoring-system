#include "Car.h"

Car car1 = {
    .movementDirection = UP,
    .operatingMode = AUTOMATIC,
    .currentFloor = 1
};

Car car2 = {
    .movementDirection = UP,
    .operatingMode = AUTOMATIC,
    .currentFloor = 1
};

Car car3 = {
    .movementDirection = UP,
    .operatingMode = AUTOMATIC,
    .currentFloor = 1
};
